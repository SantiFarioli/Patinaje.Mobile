<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true">

    <LinearLayout
        android:orientation="vertical"
        android:padding="16dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <!-- ===== DATOS DE LA PATINADORA ===== -->
        <TextView
            android:text="Datos de la patinadora"
            style="@style/Text.Subtitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="12dp"
            android:textAlignment="center"/>

        <!-- DNI -->
        <LinearLayout
            android:background="@drawable/bg_rounded_item"
            android:backgroundTint="@color/bg_item_purple"
            android:padding="12dp"
            android:layout_marginBottom="8dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal">
            <TextView android:text="🆔 DNI: " style="@style/Text.Body" />
            <TextView android:id="@+id/tvDni" style="@style/Text.Body" android:textStyle="bold"/>
        </LinearLayout>

        <!-- Domicilio -->
        <LinearLayout
            android:background="@drawable/bg_rounded_item"
            android:backgroundTint="@color/bg_item_green"
            android:padding="12dp"
            android:layout_marginBottom="8dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal">
            <TextView android:text="🏠 Domicilio: " style="@style/Text.Body" />
            <TextView android:id="@+id/tvDomicilio" style="@style/Text.Body" android:textStyle="bold"/>
        </LinearLayout>

        <!-- Nacimiento -->
        <LinearLayout
            android:background="@drawable/bg_rounded_item"
            android:backgroundTint="@color/bg_item_orange"
            android:padding="12dp"
            android:layout_marginBottom="8dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal">
            <TextView android:text="🎂 Nacimiento: " style="@style/Text.Body" />
            <TextView android:id="@+id/tvNacimiento" style="@style/Text.Body" android:textStyle="bold"/>
        </LinearLayout>

        <!-- Ficha Médica -->
        <LinearLayout
            android:background="@drawable/bg_rounded_item"
            android:backgroundTint="@color/bg_item_blue"
            android:padding="12dp"
            android:layout_marginBottom="8dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal">
            <TextView android:text="🩺 Ficha: " style="@style/Text.Body" />
            <TextView android:id="@+id/tvFicha" style="@style/Text.Body" android:textStyle="bold"/>
        </LinearLayout>

        <!-- Estado -->
        <LinearLayout
            android:background="@drawable/bg_rounded_item"
            android:backgroundTint="@color/bg_item_yellow"
            android:padding="12dp"
            android:layout_marginBottom="8dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal">
            <TextView android:text="⚡ Estado: " style="@style/Text.Body" />
            <TextView android:id="@+id/tvEstado" style="@style/Text.Body" android:textStyle="bold"/>
        </LinearLayout>

        <!-- ===== TUTORES ===== -->
        <TextView
            android:text="Tutor responsable"
            style="@style/Text.Subtitle"
            android:layout_marginTop="16dp"
            android:layout_marginBottom="8dp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textAlignment="center"/>

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recyclerTutores"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:nestedScrollingEnabled="false"/>

        <!-- Botón Volver -->
        <Button
            android:id="@+id/btnVolver"
            style="@style/Button.Primary"
            android:text="Volver"
            android:layout_marginTop="20dp"
            android:layout_gravity="center"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />

    </LinearLayout>
</ScrollView>
